const LessonStudents = () => (
  <div>
    <h1>Students</h1>
    <p>Manage your students</p>
    <div>
      {/* {lessonData.lesson?.students?.map((student: any) => (
        <div key={student.id}>
          <p>{student.name}</p>
          <p>{student.email}</p>
        </div>
      ))} */}
    </div>
  </div>
);

export default LessonStudents;
